在 sebot_ros_kits 工作空间内使用 `catkin_make` 编译后 `source devel/setup.bash` 生效；真机模式通过 `roslaunch sebot_slam sebot_auto_gmapping.launch` 启动（默认 map_topic=map），STDR 仿真模式需将 `map_topic` 改为 `slam_map`（见 `auto_slam.xml` 注释）。