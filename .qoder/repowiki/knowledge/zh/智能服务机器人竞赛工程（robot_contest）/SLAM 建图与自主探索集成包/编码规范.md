- 所有可复用功能以独立 .cpp/.h 成对拆分（如 auto_slam、frontier_search、map2costmap），公共接口统一放在 include/ 目录下供其他源文件引用。
- Launch 文件通过 `<arg>` 传递运行时开关（debug/audio/visualize/map_topic/global_frame/map_save_path），再由 `<param name="$(arg ...)"/>` 注入节点参数，保持启动与配置分离。
- 第三方 fork 包（slam_gmapping、slam_hector）保持原始目录结构与 package.xml 不变，仅在顶层 sebot_slam 包中通过 launch/include 组合调用，不修改其内部实现。