在 sebot_ros_kits 工作空间下执行 `catkin_make` 编译后 `source devel/setup.bash`；真机运行 `roslaunch sebot_navigation sebot_navigation.launch`，多点导航运行 `roslaunch sebot_navigation sebot_multinavi.launch`；仿真模式需替换传感器与里程计源（如 STDR 仿真器）。