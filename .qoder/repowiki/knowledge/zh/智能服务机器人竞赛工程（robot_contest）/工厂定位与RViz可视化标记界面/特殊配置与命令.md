在 sebot_factory 工作空间中使用 `catkin_make` 编译后 `source devel/setup.bash` 启动；通过 `roslaunch sebot_marking sebot_marking.launch` 运行，launch 文件会先启动 `map_server` 加载 `$(find sebot_navigation)/map/map.yaml`，再启动 sebot_marking 节点并传入 `pathXml` 参数指向 `$(find sebot_factory)/res/location.xml`。