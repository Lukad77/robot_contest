- 每个业务功能拆分为独立 ROS 包，包含独立的 package.xml、CMakeLists.txt、include/src/launch 目录结构
- 共享基础设施（机械臂驱动、相机、AI 推理、工具函数）集中在 include/ 与 res/ 目录，通过相对路径被各节点引用
- launch 文件采用命名前缀 sebot_ 并集中管理仿真/真机切换参数，避免硬编码路径
- 单元测试通过 unit/ 下的独立可执行文件直接 include 源码类进行功能验证，不依赖完整 ROS 节点启动