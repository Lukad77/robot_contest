- ROS 节点统一采用 rospy.init_node + Subscriber 模式，主函数中创建订阅者后调用 rospy.spin() 保持运行。
- 资源文件集中放在 res/ 子目录下（audio/match、audio/sound、images/sample），通过相对路径拼接构造完整文件路径。
- Python 脚本包不写 C++ 源码，CMakeLists.txt 仅声明 catkin 依赖，实际逻辑全部由 scripts/ 下的 .py 文件实现。
- 硬编码绝对路径指向 /root/workspace/sebot-t710-competition/... 工作空间根，便于在固定部署环境中直接运行。