- URDF 中每个 link 均同时定义 visual 与 collision 几何体，且两者引用同一 STL 文件，保证可视化与碰撞检测一致。
- 传感器与附件通过 type="fixed" 的 joint 直接固定在 base_footprint 上，不引入运动学链。
- meshes 路径统一使用 `package://sebot_urdf/meshes/<文件名>.STL` 的绝对包名引用方式。
- CMakeLists.txt 通过 foreach 循环批量 install 子目录，避免逐个重复书写 install(DIRECTORY ...) 语句。