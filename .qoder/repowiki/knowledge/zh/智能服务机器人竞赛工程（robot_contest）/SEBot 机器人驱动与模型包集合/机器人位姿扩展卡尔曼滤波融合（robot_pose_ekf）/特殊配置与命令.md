catkin 工作空间内 `catkin_make` 编译后 source `devel/setup.bash`，通过 `roslaunch robot_pose_ekf robot_pose_ekf.launch` 启动；launch 中通过 `odom_used`/`imu_used`/`vo_used`/`gps_used` 参数控制传感器接入，`freq` 设置滤波频率，`sensor_timeout` 控制传感器超时失活。